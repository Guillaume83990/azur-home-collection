/* ══ SYSTÈME I18N VILLA ══ */
var VILLA_I18N = {
    fr: {
        /* NAV */
        'nav-cta': 'Demander un devis',
        /* Breadcrumb */
        'bc-home': 'Accueil',
        'bc-location': 'Location',
        /* Hero specs */
        'spec-guests': 'Personnes',
        'spec-bedrooms': 'Chambres',
        'spec-surface': 'm²',
        'spec-pool': 'Piscine',
        /* Hero bar */
        'hero-cta': 'Demander un devis',
        'hero-contact-sep': '·',
        /* Description */
        'eyebrow-property': 'La propriété',
        'sidebar-h3': 'Intéressé par cette villa ?',
        'sidebar-sub': 'Réponse garantie sous 2 heures',
        'sidebar-cta': 'Demander un devis',
        /* Galerie */
        'eyebrow-galerie': 'Galerie',
        'galerie-h2': 'Découvrez <em>chaque espace</em>',
        /* Specs box */
        'specs-title': 'Caractéristiques',
        'spec-capacite': 'Capacité',
        'spec-chambres': 'Chambres',
        'spec-surface-lbl': 'Surface',
        'spec-piscine': 'Piscine',
        'spec-zone': 'Zone',
        'spec-conciergerie': 'Conciergerie',
        'spec-conciergerie-val': '5★ incluse',
        'spec-unit-pers': 'personnes',
        /* Conciergerie */
        'conc-eyebrow': 'Inclus dans votre séjour',
        'conc-h2': 'Conciergerie <em>5 étoiles</em>',
        'conc-chef': 'Chef privé à domicile',
        'conc-heli': 'Transferts héliportés',
        'conc-yacht': 'Location yacht & bateau',
        'conc-spa': 'Spa & bien-être à domicile',
        'conc-resto': 'Réservations restaurants & clubs',
        'conc-shop': 'Personal shopping & courses',
        'conc-btn': 'Découvrir la conciergerie →',
        /* Similaires */
        'sim-eyebrow': 'Dans la même zone',
        'sim-h2': 'Villas <em>similaires</em>',
        'sim-unit': 'pers.',
        /* CTA final */
        'cta-eyebrow': 'Prêt à réserver ?',
        'cta-prefix': 'Réserver ',
        'cta-sub': 'Disponibilités et tarifs sur demande · Réponse sous 2h',
        'cta-btn': 'Demander un devis',
        'cta-tel': 'Pascal · +33 6 00 00 00 01',
        /* Footer */
        'ft-services': 'Services',
        'ft-destinations': 'Destinations',
        'ft-societe': 'Société',
        'ft-contact': 'Contact',
        'ft-location': 'Location',
        'ft-gestion': 'Gestion',
        'ft-conciergerie': 'Conciergerie',
        'ft-a-propos': 'À propos',
        'ft-contact-link': 'Contact',
        'ft-blog': 'Blog',
        'ft-copyright': '© 2026 Azur Villa Prestige. Tous droits réservés.',
        'ft-credit': 'Conçu & développé par',
        'ft-legal': 'Mentions légales',
        'ft-privacy': 'Confidentialité',
        'ft-cgv': 'CGV',
        /* Modale */
        'modal-eyebrow': 'Demande de séjour',
        'modal-sub': 'Remplissez vos informations — notre équipe vous rappelle sous 2 heures.',
        'lbl-prenom': 'Prénom',
        'lbl-nom': 'Nom',
        'lbl-email': 'Email',
        'lbl-tel': 'Téléphone',
        'lbl-arrival': "Date d'arrivée",
        'lbl-departure': 'Date de départ',
        'lbl-guests': 'Nombre de voyageurs',
        'lbl-msg': 'Message (optionnel)',
        'ph-prenom': 'Marie',
        'ph-nom': 'Dupont',
        'ph-email': 'marie@email.com',
        'ph-tel': '+33 6 xx xx xx xx',
        'ph-guests': 'Ex : 6',
        'ph-msg': 'Vos souhaits particuliers, questions...',
        'rgpd-check': "J'accepte que mes données soient utilisées pour traiter ma demande.",
        'rgpd-link': 'Politique de confidentialité',
        'submit-btn': 'Envoyer ma demande',
        'submit-loading': 'Envoi en cours...',
        'rgpd-note': '🔒 Données protégées · Réponse garantie sous 2 heures',
        'conf-msg': 'Votre demande a bien été prise en compte.<br>Notre équipe va <strong>rapidement revenir vers vous</strong> pour organiser votre séjour sur-mesure.',
        'conf-sign': "— L'équipe Azur Villa Prestige",
        'conf-close': 'Fermer',
        /* Majordome */
        'maj-online': 'En ligne',
        'maj-q': 'Intéressé par cette villa ?',
        'maj-devis': '📋 Demander un devis',
        /* Lang */
        'lang-label': 'Sélection de langue',
    },
    en: {
        'nav-cta': 'Request a quote',
        'bc-home': 'Home',
        'bc-location': 'Rentals',
        'spec-guests': 'Guests',
        'spec-bedrooms': 'Bedrooms',
        'spec-surface': 'sqm',
        'spec-pool': 'Pool',
        'hero-cta': 'Request a quote',
        'hero-contact-sep': '·',
        'eyebrow-property': 'The property',
        'sidebar-h3': 'Interested in this villa?',
        'sidebar-sub': 'Response guaranteed within 2 hours',
        'sidebar-cta': 'Request a quote',
        'eyebrow-galerie': 'Gallery',
        'galerie-h2': 'Discover <em>every space</em>',
        'specs-title': 'Features',
        'spec-capacite': 'Capacity',
        'spec-chambres': 'Bedrooms',
        'spec-surface-lbl': 'Surface',
        'spec-piscine': 'Pool',
        'spec-zone': 'Area',
        'spec-conciergerie': 'Concierge',
        'spec-conciergerie-val': '5★ included',
        'spec-unit-pers': 'guests',
        'conc-eyebrow': 'Included in your stay',
        'conc-h2': '<em>5-star</em> concierge',
        'conc-chef': 'Private chef at home',
        'conc-heli': 'Helicopter transfers',
        'conc-yacht': 'Yacht & boat rental',
        'conc-spa': 'Spa & wellness at home',
        'conc-resto': 'Restaurant & club bookings',
        'conc-shop': 'Personal shopping & groceries',
        'conc-btn': 'Discover our concierge →',
        'sim-eyebrow': 'In the same area',
        'sim-h2': '<em>Similar</em> villas',
        'sim-unit': 'guests',
        'cta-eyebrow': 'Ready to book?',
        'cta-prefix': 'Book ',
        'cta-sub': 'Availability and rates on request · Reply within 2h',
        'cta-btn': 'Request a quote',
        'cta-tel': 'Pascal · +33 6 00 00 00 01',
        'ft-services': 'Services',
        'ft-destinations': 'Destinations',
        'ft-societe': 'Company',
        'ft-contact': 'Contact',
        'ft-location': 'Rentals',
        'ft-gestion': 'Management',
        'ft-conciergerie': 'Concierge',
        'ft-a-propos': 'About',
        'ft-contact-link': 'Contact',
        'ft-blog': 'Blog',
        'ft-copyright': '© 2026 Azur Villa Prestige. All rights reserved.',
        'ft-credit': 'Designed & developed by',
        'ft-legal': 'Legal notice',
        'ft-privacy': 'Privacy',
        'ft-cgv': 'T&Cs',
        'modal-eyebrow': 'Stay request',
        'modal-sub': 'Fill in your details — our team will call you back within 2 hours.',
        'lbl-prenom': 'First name',
        'lbl-nom': 'Last name',
        'lbl-email': 'Email',
        'lbl-tel': 'Phone',
        'lbl-arrival': 'Arrival date',
        'lbl-departure': 'Departure date',
        'lbl-guests': 'Number of guests',
        'lbl-msg': 'Message (optional)',
        'ph-prenom': 'Jane',
        'ph-nom': 'Smith',
        'ph-email': 'jane@email.com',
        'ph-tel': '+33 6 xx xx xx xx',
        'ph-guests': 'E.g. 6',
        'ph-msg': 'Your special requests, questions...',
        'rgpd-check': 'I agree that my data may be used to process my request.',
        'rgpd-link': 'Privacy policy',
        'submit-btn': 'Send my request',
        'submit-loading': 'Sending...',
        'rgpd-note': '🔒 Protected data · Response guaranteed within 2 hours',
        'conf-msg': 'Your request has been received.<br>Our team will <strong>get back to you shortly</strong> to organise your bespoke stay.',
        'conf-sign': '— The Azur Villa Prestige team',
        'conf-close': 'Close',
        'maj-online': 'Online',
        'maj-q': 'Interested in this villa?',
        'maj-devis': '📋 Request a quote',
        'lang-label': 'Language selection',
    },
    de: {
        'nav-cta': 'Angebot anfragen',
        'bc-home': 'Startseite',
        'bc-location': 'Miete',
        'spec-guests': 'Personen',
        'spec-bedrooms': 'Zimmer',
        'spec-surface': 'm²',
        'spec-pool': 'Pool',
        'hero-cta': 'Angebot anfragen',
        'hero-contact-sep': '·',
        'eyebrow-property': 'Die Immobilie',
        'sidebar-h3': 'Interesse an dieser Villa?',
        'sidebar-sub': 'Antwort garantiert innerhalb von 2 Stunden',
        'sidebar-cta': 'Angebot anfragen',
        'eyebrow-galerie': 'Galerie',
        'galerie-h2': 'Entdecken Sie <em>jeden Raum</em>',
        'specs-title': 'Eigenschaften',
        'spec-capacite': 'Kapazität',
        'spec-chambres': 'Zimmer',
        'spec-surface-lbl': 'Fläche',
        'spec-piscine': 'Pool',
        'spec-zone': 'Zone',
        'spec-conciergerie': 'Concierge',
        'spec-conciergerie-val': '5★ inklusive',
        'spec-unit-pers': 'Personen',
        'conc-eyebrow': 'In Ihrem Aufenthalt enthalten',
        'conc-h2': 'Concierge <em>5 Sterne</em>',
        'conc-chef': 'Privater Koch zu Hause',
        'conc-heli': 'Hubschrauber-Transfers',
        'conc-yacht': 'Yacht- & Bootsverleih',
        'conc-spa': 'Spa & Wellness zu Hause',
        'conc-resto': 'Restaurant- & Clubreservierungen',
        'conc-shop': 'Personal Shopping & Einkäufe',
        'conc-btn': 'Concierge entdecken →',
        'sim-eyebrow': 'In der gleichen Zone',
        'sim-h2': 'Ähnliche <em>Villen</em>',
        'sim-unit': 'Pers.',
        'cta-eyebrow': 'Bereit zu buchen?',
        'cta-prefix': 'Buchen ',
        'cta-sub': 'Verfügbarkeiten und Preise auf Anfrage · Antwort innerhalb von 2 Stunden',
        'cta-btn': 'Angebot anfragen',
        'cta-tel': 'Pascal · +33 6 00 00 00 01',
        'ft-services': 'Dienstleistungen',
        'ft-destinations': 'Reiseziele',
        'ft-societe': 'Unternehmen',
        'ft-contact': 'Kontakt',
        'ft-location': 'Miete',
        'ft-gestion': 'Verwaltung',
        'ft-conciergerie': 'Concierge',
        'ft-a-propos': 'Über uns',
        'ft-contact-link': 'Kontakt',
        'ft-blog': 'Blog',
        'ft-copyright': '© 2026 Azur Villa Prestige. Alle Rechte vorbehalten.',
        'ft-credit': 'Konzipiert & entwickelt von',
        'ft-legal': 'Impressum',
        'ft-privacy': 'Datenschutz',
        'ft-cgv': 'AGB',
        'modal-eyebrow': 'Aufenthaltsanfrage',
        'modal-sub': 'Füllen Sie Ihre Daten aus — unser Team ruft Sie innerhalb von 2 Stunden zurück.',
        'lbl-prenom': 'Vorname',
        'lbl-nom': 'Nachname',
        'lbl-email': 'E-Mail',
        'lbl-tel': 'Telefon',
        'lbl-arrival': 'Anreisedatum',
        'lbl-departure': 'Abreisedatum',
        'lbl-guests': 'Anzahl Reisende',
        'lbl-msg': 'Nachricht (optional)',
        'ph-prenom': 'Maria',
        'ph-nom': 'Müller',
        'ph-email': 'maria@email.com',
        'ph-tel': '+33 6 xx xx xx xx',
        'ph-guests': 'Z.B. 6',
        'ph-msg': 'Ihre besonderen Wünsche, Fragen...',
        'rgpd-check': 'Ich stimme zu, dass meine Daten zur Bearbeitung meiner Anfrage verwendet werden.',
        'rgpd-link': 'Datenschutzrichtlinie',
        'submit-btn': 'Anfrage senden',
        'submit-loading': 'Wird gesendet...',
        'rgpd-note': '🔒 Daten geschützt · Antwort garantiert innerhalb 2 Stunden',
        'conf-msg': 'Ihre Anfrage wurde erhalten.<br>Unser Team wird sich <strong>in Kürze bei Ihnen melden</strong>, um Ihren maßgeschneiderten Aufenthalt zu organisieren.',
        'conf-sign': '— Das Team von Azur Villa Prestige',
        'conf-close': 'Schließen',
        'maj-online': 'Online',
        'maj-q': 'Interesse an dieser Villa?',
        'maj-devis': '📋 Angebot anfragen',
        'lang-label': 'Sprachauswahl',
    },
    it: {
        'nav-cta': 'Richiedere un preventivo',
        'bc-home': 'Home',
        'bc-location': 'Affitti',
        'spec-guests': 'Persone',
        'spec-bedrooms': 'Camere',
        'spec-surface': 'm²',
        'spec-pool': 'Piscina',
        'hero-cta': 'Richiedere un preventivo',
        'hero-contact-sep': '·',
        'eyebrow-property': 'La proprietà',
        'sidebar-h3': 'Interessato a questa villa?',
        'sidebar-sub': 'Risposta garantita entro 2 ore',
        'sidebar-cta': 'Richiedere un preventivo',
        'eyebrow-galerie': 'Galleria',
        'galerie-h2': 'Scoprite <em>ogni spazio</em>',
        'specs-title': 'Caratteristiche',
        'spec-capacite': 'Capacità',
        'spec-chambres': 'Camere',
        'spec-surface-lbl': 'Superficie',
        'spec-piscine': 'Piscina',
        'spec-zone': 'Zona',
        'spec-conciergerie': 'Concierge',
        'spec-conciergerie-val': '5★ incluso',
        'spec-unit-pers': 'persone',
        'conc-eyebrow': 'Incluso nel vostro soggiorno',
        'conc-h2': 'Concierge <em>5 stelle</em>',
        'conc-chef': 'Chef privato a domicilio',
        'conc-heli': 'Transfer in elicottero',
        'conc-yacht': 'Noleggio yacht & barca',
        'conc-spa': 'Spa & benessere a domicilio',
        'conc-resto': 'Prenotazioni ristoranti & club',
        'conc-shop': 'Personal shopping & spesa',
        'conc-btn': 'Scopri il concierge →',
        'sim-eyebrow': 'Nella stessa zona',
        'sim-h2': 'Ville <em>simili</em>',
        'sim-unit': 'pers.',
        'cta-eyebrow': 'Pronto a prenotare?',
        'cta-prefix': 'Prenotare ',
        'cta-sub': 'Disponibilità e tariffe su richiesta · Risposta entro 2h',
        'cta-btn': 'Richiedere un preventivo',
        'cta-tel': 'Pascal · +33 6 00 00 00 01',
        'ft-services': 'Servizi',
        'ft-destinations': 'Destinazioni',
        'ft-societe': 'Società',
        'ft-contact': 'Contatto',
        'ft-location': 'Affitti',
        'ft-gestion': 'Gestione',
        'ft-conciergerie': 'Concierge',
        'ft-a-propos': 'Chi siamo',
        'ft-contact-link': 'Contatto',
        'ft-blog': 'Blog',
        'ft-copyright': '© 2026 Azur Villa Prestige. Tutti i diritti riservati.',
        'ft-credit': 'Progettato & sviluppato da',
        'ft-legal': 'Note legali',
        'ft-privacy': 'Privacy',
        'ft-cgv': 'CGV',
        'modal-eyebrow': 'Richiesta di soggiorno',
        'modal-sub': 'Compilate i vostri dati — il nostro team vi richiamerà entro 2 ore.',
        'lbl-prenom': 'Nome',
        'lbl-nom': 'Cognome',
        'lbl-email': 'Email',
        'lbl-tel': 'Telefono',
        'lbl-arrival': 'Data di arrivo',
        'lbl-departure': 'Data di partenza',
        'lbl-guests': 'Numero di ospiti',
        'lbl-msg': 'Messaggio (opzionale)',
        'ph-prenom': 'Maria',
        'ph-nom': 'Rossi',
        'ph-email': 'maria@email.com',
        'ph-tel': '+33 6 xx xx xx xx',
        'ph-guests': 'Es. 6',
        'ph-msg': 'Le vostre richieste particolari, domande...',
        'rgpd-check': 'Accetto che i miei dati vengano utilizzati per elaborare la mia richiesta.',
        'rgpd-link': 'Informativa sulla privacy',
        'submit-btn': 'Invia la mia richiesta',
        'submit-loading': 'Invio in corso...',
        'rgpd-note': '🔒 Dati protetti · Risposta garantita entro 2 ore',
        'conf-msg': 'La vostra richiesta è stata ricevuta.<br>Il nostro team <strong>vi risponderà presto</strong> per organizzare il vostro soggiorno su misura.',
        'conf-sign': '— Il team di Azur Villa Prestige',
        'conf-close': 'Chiudi',
        'maj-online': 'Online',
        'maj-q': 'Interessato a questa villa?',
        'maj-devis': '📋 Richiedere un preventivo',
        'lang-label': 'Selezione lingua',
    }
};

/* Langue active */
window.VILLA_LANG = 'fr';

/* Appliquer la traduction */
window.applyVillaLang = function (lang) {
    if (!VILLA_I18N[lang]) return;
    window.VILLA_LANG = lang;
    var t = VILLA_I18N[lang];

    /* Lang switcher : bouton actif */
    document.querySelectorAll('.lang-sel .lb').forEach(function (btn) {
        btn.classList.remove('active');
        btn.removeAttribute('aria-current');
    });
    var labels = { fr: 'Français', en: 'English', de: 'Deutsch', it: 'Italiano' };
    var activeBtn = document.querySelector('.lang-sel .lb[aria-label="' + labels[lang] + '"]') ||
        document.querySelector('#lb-' + lang);
    if (!activeBtn && lang === 'fr') activeBtn = document.querySelector('.lang-sel .lb:first-child');
    if (activeBtn) { activeBtn.classList.add('active'); activeBtn.setAttribute('aria-current', 'true'); }

    /* Nav CTA */
    var navCta = document.querySelector('.nav-cta');
    if (navCta) navCta.textContent = t['nav-cta'];

    /* Breadcrumb */
    var bcHome = document.querySelector('.vd-breadcrumb a:first-child');
    if (bcHome) bcHome.textContent = t['bc-home'];
    var bcLoc = document.querySelector('.vd-breadcrumb a:nth-child(3)');
    if (bcLoc) { bcLoc.textContent = t['bc-location']; bcLoc.href = lang === 'fr' ? '../location.html' : '../location.html'; }

    /* Hero bar CTA */
    var hBarCta = document.querySelector('.vd-hero-bar .vd-btn-primary');
    if (hBarCta) { var svg = hBarCta.querySelector('svg'); hBarCta.textContent = t['hero-cta']; if (svg) hBarCta.insertBefore(svg, hBarCta.firstChild); }

    /* Description eyebrow */
    var eyebrow = document.querySelector('#vd-desc-left .eyebrow');
    if (eyebrow) eyebrow.textContent = t['eyebrow-property'];

    /* Sidebar */
    var sbh3 = document.querySelector('.vd-sidebar-cta h3');
    if (sbh3) sbh3.textContent = t['sidebar-h3'];
    var sbp = document.querySelector('.vd-sidebar-cta > p');
    if (sbp) sbp.textContent = t['sidebar-sub'];
    var sbCta = document.querySelector('.vd-sidebar-cta .vd-btn-primary');
    if (sbCta) { var svg2 = sbCta.querySelector('svg'); sbCta.textContent = t['sidebar-cta']; if (svg2) sbCta.insertBefore(svg2, sbCta.firstChild); }

    /* Galerie */
    var galEyebrow = document.querySelector('.vd-galerie-head .eyebrow');
    if (galEyebrow) galEyebrow.textContent = t['eyebrow-galerie'];
    var galH2 = document.querySelector('.vd-galerie-head h2');
    if (galH2) galH2.innerHTML = t['galerie-h2'];

    /* Specs box - rebuilt dynamiquement donc on traduit via les données */

    /* Conciergerie */
    var concEy = document.querySelector('.vd-conc .eyebrow');
    if (concEy) concEy.textContent = t['conc-eyebrow'];
    var concH2 = document.querySelector('.vd-conc h2');
    if (concH2) concH2.innerHTML = t['conc-h2'];
    var concItems = document.querySelectorAll('.vd-conc-item p');
    var concTexts = ['conc-chef', 'conc-heli', 'conc-yacht', 'conc-spa', 'conc-resto', 'conc-shop'];
    concItems.forEach(function (p, i) { if (concTexts[i]) p.textContent = t[concTexts[i]]; });
    var concBtn = document.querySelector('.vd-btn-ghost[href*="conciergerie"]');
    if (concBtn) concBtn.textContent = t['conc-btn'];

    /* Similaires */
    var simEy = document.querySelector('.vd-similaires .eyebrow');
    if (simEy) simEy.textContent = t['sim-eyebrow'];
    var simH2 = document.querySelector('.vd-similaires h2');
    if (simH2) simH2.innerHTML = t['sim-h2'];

    /* CTA final */
    var ctaEy = document.querySelector('.vd-cta-final .eyebrow');
    if (ctaEy) ctaEy.textContent = t['cta-eyebrow'];
    var ctaSub = document.querySelector('.vd-cta-inner > p');
    if (ctaSub) ctaSub.textContent = t['cta-sub'];
    var ctaBtn = document.querySelector('.vd-cta-btns .vd-btn-primary');
    if (ctaBtn) ctaBtn.textContent = t['cta-btn'];

    /* Footer */
    var ftTitles = document.querySelectorAll('.fn-col h4');
    var ftKeys = ['ft-services', 'ft-destinations', 'ft-societe', 'ft-contact'];
    ftTitles.forEach(function (h4, i) { if (ftKeys[i]) h4.textContent = t[ftKeys[i]]; });
    /* Footer liens texte */
    var ftLinks = [
        ['.fn-col:nth-child(1) a:nth-child(2)', 'ft-location'],
        ['.fn-col:nth-child(1) a:nth-child(3)', 'ft-gestion'],
        ['.fn-col:nth-child(1) a:nth-child(4)', 'ft-conciergerie'],
        ['.fn-col:nth-child(3) a:nth-child(2)', 'ft-a-propos'],
        ['.fn-col:nth-child(3) a:nth-child(3)', 'ft-contact-link'],
        ['.fn-col:nth-child(3) a:nth-child(4)', 'ft-blog'],
    ];
    ftLinks.forEach(function (pair) {
        var el = document.querySelector(pair[0]);
        if (el) el.textContent = t[pair[1]];
    });
    var ftCopy = document.querySelector('.ft-bot > span:first-child');
    if (ftCopy) ftCopy.textContent = t['ft-copyright'];
    var ftCreditSpan = document.querySelector('.ft-credit');
    if (ftCreditSpan) {
        var sudLink = ftCreditSpan.querySelector('a');
        ftCreditSpan.textContent = t['ft-credit'] + ' ';
        if (sudLink) ftCreditSpan.appendChild(sudLink);
    }
    var ftLegalLinks = document.querySelectorAll('.ft-legal a');
    if (ftLegalLinks[0]) ftLegalLinks[0].textContent = t['ft-legal'];
    if (ftLegalLinks[1]) ftLegalLinks[1].textContent = t['ft-privacy'];
    if (ftLegalLinks[2]) ftLegalLinks[2].textContent = t['ft-cgv'];

    /* Modale */
    var modalEy = document.querySelector('.bm-eyebrow');
    if (modalEy) modalEy.textContent = t['modal-eyebrow'];
    var modalSub = document.querySelector('.bm-sub');
    if (modalSub) modalSub.textContent = t['modal-sub'];
    /* Labels */
    var lblMap = [
        ['bm-prenom', 'lbl-prenom', 'ph-prenom'],
        ['bm-nom', 'lbl-nom', 'ph-nom'],
        ['bm-email', 'lbl-email', 'ph-email'],
        ['bm-tel', 'lbl-tel', 'ph-tel'],
        ['bm-arrival', 'lbl-arrival', null],
        ['bm-departure', 'lbl-departure', null],
        ['bm-guests', 'lbl-guests', 'ph-guests'],
        ['bm-msg', 'lbl-msg', 'ph-msg'],
    ];
    lblMap.forEach(function (m) {
        var lbl = document.querySelector('label[for="' + m[0] + '"]');
        var inp = document.getElementById(m[0]);
        if (lbl) { var req = lbl.querySelector('.bm-req'); lbl.childNodes[0].textContent = t[m[1]] + ' '; if (req) lbl.appendChild(req); else lbl.textContent = t[m[1]]; }
        if (inp && m[2]) inp.placeholder = t[m[2]];
    });
    var rgpdSpan = document.querySelector('.bm-check-label > span');
    if (rgpdSpan) {
        var rgpdA = rgpdSpan.querySelector('a');
        rgpdSpan.textContent = t['rgpd-check'] + ' ';
        if (rgpdA) { rgpdA.textContent = t['rgpd-link']; rgpdSpan.appendChild(rgpdA); }
    }
    var submitTxt = document.getElementById('bm-submit-txt');
    if (submitTxt) submitTxt.textContent = t['submit-btn'];
    var rgpdNote = document.querySelector('.bm-rgpd-note');
    if (rgpdNote) rgpdNote.textContent = t['rgpd-note'];
    var confMsg = document.querySelector('.bm-conf-msg');
    if (confMsg) confMsg.innerHTML = t['conf-msg'];
    var confSign = document.querySelector('.bm-conf-sign');
    if (confSign) confSign.textContent = t['conf-sign'];
    var confClose = document.querySelector('.bm-conf-close');
    if (confClose) confClose.textContent = t['conf-close'];

    /* Majordome */
    var majOnline = document.querySelector('.maj-st span:last-child');
    if (majOnline) majOnline.textContent = t['maj-online'];
    var majQ = document.querySelector('#mjs-0 .mj-q');
    if (majQ) majQ.textContent = t['maj-q'];
    var majDevis = document.querySelector('#mjs-0 .mj-ch button:first-child');
    if (majDevis) majDevis.textContent = t['maj-devis'];

    /* HTML lang attribute */
    var htmlEl = document.querySelector('html');
    if (htmlEl) htmlEl.setAttribute('lang', lang);

    /* Mémoriser dans sessionStorage */
    try { sessionStorage.setItem('villa-lang', lang); } catch (e) { }
};

/* Initialisation lang switcher */
window.initVillaLang = function () {
    /* Langue déjà détectée au début de DOMContentLoaded */
    var lang = window.VILLA_LANG || 'fr';

    /* Boutons lang switcher */
    var btns = document.querySelectorAll('#villa-lang-sel .lb');
    btns.forEach(function (btn) {
        btn.addEventListener('click', function () {
            var l = btn.getAttribute('aria-label');
            var map = {
                'Français': 'fr', 'French': 'fr', 'Français': 'fr',
                'English': 'en', 'Deutsch': 'de', 'Italiano': 'it'
            };
            var langCode = map[l];
            if (langCode) window.applyVillaLang(langCode);
        });
    });

    /* Langue détectée via URL — les textes HTML sont déjà traduits statiquement */
};

/* ============================================================
   AZUR VILLA PRESTIGE — js/villa.js
   Fiche villa dynamique + modale réservation GSAP
   ============================================================ */

/* ══ MODALE RÉSERVATION ══ */
window.openBookingModal = function () {
    var modal = document.getElementById('booking-modal');
    if (!modal) return;
    modal.setAttribute('aria-hidden', 'false');
    modal.style.display = 'flex';
    document.body.style.overflow = 'hidden';
    if (typeof gsap !== 'undefined') {
        gsap.fromTo('.bm-box',
            { opacity: 0, y: 40, scale: .96 },
            { opacity: 1, y: 0, scale: 1, duration: .5, ease: 'power3.out' }
        );
    }
    if (typeof gtag === 'function') gtag('event', 'booking_open', { event_category: 'Villa' });
};

window.closeBookingModal = function () {
    var modal = document.getElementById('booking-modal');
    if (!modal) return;
    if (typeof gsap !== 'undefined') {
        gsap.to('.bm-box', {
            opacity: 0, y: 20, scale: .97, duration: .35, ease: 'power2.in',
            onComplete: function () {
                modal.style.display = 'none';
                modal.setAttribute('aria-hidden', 'true');
                document.body.style.overflow = '';
            }
        });
    } else {
        modal.style.display = 'none';
        modal.setAttribute('aria-hidden', 'true');
        document.body.style.overflow = '';
    }
};

window.submitBooking = function (e) {
    e.preventDefault();
    var prenom = document.getElementById('bm-prenom').value.trim();
    var nom = document.getElementById('bm-nom').value.trim();
    var email = document.getElementById('bm-email').value.trim();
    var tel = document.getElementById('bm-tel').value.trim();
    var arrival = document.getElementById('bm-arrival').value;
    var departure = document.getElementById('bm-departure').value;
    var guests = document.getElementById('bm-guests').value;
    var msg = document.getElementById('bm-msg').value.trim();
    var rgpd = document.getElementById('bm-rgpd').checked;
    var villaName = document.getElementById('bm-villa-name') ? document.getElementById('bm-villa-name').textContent : '';

    if (!prenom || !nom || !email || !tel || !rgpd) {
        /* Shake les champs vides */
        if (typeof gsap !== 'undefined') {
            var empties = [];
            if (!prenom) empties.push('#bm-prenom');
            if (!nom) empties.push('#bm-nom');
            if (!email) empties.push('#bm-email');
            if (!tel) empties.push('#bm-tel');
            if (empties.length) {
                gsap.to(empties.join(','), {
                    x: [-6, 6, -4, 4, -2, 2, 0], duration: .5, ease: 'power2.inOut'
                });
            }
        }
        return;
    }

    var btn = document.getElementById('bm-submit');
    var txt = document.getElementById('bm-submit-txt');
    btn.disabled = true;
    txt.textContent = 'Envoi en cours...';

    /* Message WhatsApp formaté */
    var t = VILLA_I18N[window.VILLA_LANG || 'fr'];
    /* Remettre le texte du bouton submit dans la bonne langue */
    if (txt) txt.textContent = t['submit-loading'] || txt.textContent;
    var waMsg = 'Demande de sejour - Azur Villa Prestige'
        + '\n\nVilla : ' + villaName
        + '\nPrenom : ' + prenom
        + '\nNom : ' + nom
        + '\nEmail : ' + email
        + '\nTel : ' + tel
        + (arrival ? '\nArrivee : ' + arrival : '')
        + (departure ? '\nDepart : ' + departure : '')
        + (guests ? '\nVoyageurs : ' + guests : '')
        + (msg ? '\n\nMessage : ' + msg : '');

    /* Envoyer via WhatsApp */
    window.open('https://wa.me/+33600000001?text=' + encodeURIComponent(waMsg), '_blank');

    /* Afficher la confirmation */
    showBookingConfirm(prenom);
    if (typeof gtag === 'function') gtag('event', 'booking_submit', { event_category: 'Villa', event_label: villaName });
};

function showBookingConfirm(prenom) {
    var formWrap = document.getElementById('bm-form-wrap');
    var confirm = document.getElementById('bm-confirm');
    var confPrenom = document.getElementById('bm-conf-prenom');
    if (!formWrap || !confirm) return;

    if (confPrenom) confPrenom.textContent = prenom;

    if (typeof gsap !== 'undefined') {
        gsap.to(formWrap, {
            opacity: 0, y: -20, duration: .35, ease: 'power2.in',
            onComplete: function () {
                formWrap.style.display = 'none';
                confirm.style.display = 'flex';
                /* Animer la confirmation */
                gsap.from('.bm-confirm-inner > *', {
                    opacity: 0, y: 24, duration: .6, stagger: .1, ease: 'power3.out'
                });
                /* Cercle SVG animé */
                var circle = confirm.querySelector('.bm-circle');
                var tick = confirm.querySelector('.bm-tick');
                if (circle) {
                    var cLen = 2 * Math.PI * 30;
                    gsap.set(circle, { strokeDasharray: cLen, strokeDashoffset: cLen });
                    gsap.to(circle, { strokeDashoffset: 0, duration: .8, ease: 'power3.out' });
                }
                if (tick) {
                    var tLen = 40;
                    gsap.set(tick, { strokeDasharray: tLen, strokeDashoffset: tLen });
                    gsap.to(tick, { strokeDashoffset: 0, duration: .5, delay: .65, ease: 'power3.out' });
                }
            }
        });
    } else {
        formWrap.style.display = 'none';
        confirm.style.display = 'flex';
    }
}

/* ══ CONSTRUCTION DYNAMIQUE DE LA PAGE ══ */
document.addEventListener('DOMContentLoaded', function () {

    /* 1. Lire l'id dans l'URL */
    var params = new URLSearchParams(window.location.search);
    var villaId = params.get('id');

    if (!villaId || !window.VILLAS) return;
    var villa = window.getVilla(villaId);
    if (!villa) {
        document.getElementById('vd-h1').textContent = 'Villa introuvable';
        return;
    }

    /* Détecter la langue depuis l'URL AVANT de construire la page */
    var _pathLang = 'fr';
    var _path = window.location.pathname;
    if (_path.indexOf('/en/') !== -1) _pathLang = 'en';
    else if (_path.indexOf('/de/') !== -1) _pathLang = 'de';
    else if (_path.indexOf('/it/') !== -1) _pathLang = 'it';
    window.VILLA_LANG = _pathLang;
    var t = VILLA_I18N[_pathLang] || VILLA_I18N['fr'];

    /* 2. SEO */
    document.getElementById('page-title').textContent =
        villa.name + ' — ' + villa.location + ' · Azur Villa Prestige';
    document.getElementById('page-desc').setAttribute('content',
        villa.name + ' — ' + villa.location + '. ' + villa.specs.guests + ' personnes, ' + villa.specs.surface + ' m².');

    /* 3. Hero */
    var heroImg = document.getElementById('vd-hero-img');
    heroImg.src = villa.image;
    heroImg.alt = villa.name + ' — ' + villa.location;

    document.getElementById('vd-bc-name').textContent = villa.name;
    document.getElementById('vd-badge').textContent = villa.badge;
    document.getElementById('vd-h1').textContent = villa.name;
    document.getElementById('vd-location').textContent = villa.location;

    /* Specs hero */
    var heroSpecs = document.getElementById('vd-hero-specs');
    [
        { n: villa.specs.guests, l: t['spec-guests'] || 'Personnes' },
        { n: villa.specs.bedrooms, l: t['spec-bedrooms'] || 'Chambres' },
        { n: villa.specs.surface, l: t['spec-surface'] || 'm²' },
        { n: villa.specs.pool, l: t['spec-pool'] || 'Piscine' }
    ].forEach(function (s, i, arr) {
        heroSpecs.innerHTML += '<div class="vd-hs-item"><span class="vd-hs-n">' + s.n + '</span><span class="vd-hs-l">' + s.l + '</span></div>';
        if (i < arr.length - 1) heroSpecs.innerHTML += '<div class="vd-hs-sep"></div>';
    });

    /* 4. Modale — nom de la villa */
    var bmTitle = document.getElementById('bm-villa-name');
    if (bmTitle) bmTitle.innerHTML = villa.name + ' — <em>demande de séjour</em>';

    /* 5. Description */
    document.getElementById('vd-desc-title').textContent = villa.desc_title;
    document.getElementById('vd-desc-text').textContent = villa.desc;

    /* Équipements */
    document.getElementById('vd-ams').innerHTML = villa.amenities.map(function (a) {
        return '<span class="vd-am-tag">' + a + '</span>';
    }).join('');

    /* Specs box */
    var specsBox = document.getElementById('vd-specs-box');
    var specsData = [
        [t['spec-capacite'] || 'Capacité', villa.specs.guests + ' ' + (t['spec-unit-pers'] || 'personnes')],
        [t['spec-chambres'] || 'Chambres', villa.specs.bedrooms],
        [t['spec-surface-lbl'] || 'Surface', villa.specs.surface + ' m²'],
        [t['spec-piscine'] || 'Piscine', villa.specs.pool],
        [t['spec-zone'] || 'Zone', villa.location],
        [t['spec-conciergerie'] || 'Conciergerie', t['spec-conciergerie-val'] || '5★ incluse']
    ];
    specsBox.innerHTML = '<h3>' + (t['specs-title'] || 'Caractéristiques') + '</h3>' +
        specsData.map(function (r) {
            return '<div class="vd-spec-row"><span>' + r[0] + '</span><strong>' + r[1] + '</strong></div>';
        }).join('');

    /* 6. Galerie avec légendes */
    var galGrid = document.getElementById('vd-galerie-grid');
    var photos = villa.photos || [];
    galGrid.innerHTML = photos.map(function (p, i) {
        return '<div class="vd-gal-item" data-gsap-gal>' +
            '<div class="vd-gal-img-wrap">' +
            '<img src="' + p.src + '" alt="' + villa.name + ' — ' + p.legend + '" loading="' + (i === 0 ? 'eager' : 'lazy') + '" decoding="async"/>' +
            '</div>' +
            '<div class="vd-gal-legend">' +
            '<span class="vd-gal-num">0' + (i + 1) + '</span>' +
            '<span class="vd-gal-text">' + p.legend + '</span>' +
            '</div>' +
            '</div>';
    }).join('');

    /* 7. CTA titre */
    document.getElementById('vd-cta-title').textContent = (t['cta-prefix'] || 'Réserver ') + villa.name;

    /* 8. Villas similaires */
    var similaires = window.getSimilaires(villaId, villa.zone);
    document.getElementById('vd-sim-grid').innerHTML = similaires.map(function (v) {
        return '<a href="villa.html?id=' + v.id + '" class="vd-sim-card">' +
            '<div class="vd-sim-img"><img src="' + v.image + '" alt="' + v.name + '" loading="lazy"/></div>' +
            '<div class="vd-sim-body">' +
            '<span class="vd-sim-location">' + v.location + '</span>' +
            '<h3>' + v.name + '</h3>' +
            '<span class="vd-sim-specs">' + v.specs.guests + ' ' + (t['sim-unit'] || 'pers.') + ' · ' + v.specs.surface + ' m²</span>' +
            '</div>' +
            '</a>';
    }).join('');

    /* 8b. Initialiser le système de traduction */
    window.initVillaLang();

    /* 9. GSAP animations */
    if (typeof gsap === 'undefined') return;
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    gsap.registerPlugin(ScrollTrigger);

    /* Hero Ken Burns */
    gsap.fromTo(heroImg,
        { scale: 1.08 },
        { scale: 1.0, duration: 2.4, ease: 'power2.out' }
    );
    /* parallax scrub supprimé — TBT + assombrissement */

    /* Hero texte cascade */
    var tl = gsap.timeline({ delay: .2 });
    tl.from('.vd-breadcrumb', { opacity: 0, y: 10, duration: .5, ease: 'power3.out', clearProps: 'all' })
        .from('.vd-badge', { opacity: 0, y: 14, duration: .5, clearProps: 'all' }, '-=.2')
        .from('.vd-h1', { opacity: 0, y: 28, duration: .7, ease: 'power4.out', clearProps: 'all' }, '-=.2')
        .from('.vd-location', { opacity: 0, y: 12, duration: .5, clearProps: 'all' }, '-=.3')
        .from('.vd-hero-specs', { opacity: 0, y: 16, duration: .6, clearProps: 'all' }, '-=.2')
        .from('.vd-hero-bar', { opacity: 0, y: 20, duration: .5, clearProps: 'all' }, '-=.2');

    /* Description */
    gsap.from('#vd-desc-left > *', {
        scrollTrigger: { trigger: '#vd-desc', start: 'top 76%', once: true },
        opacity: 0, y: 28, duration: .8, stagger: .1, ease: 'power3.out', clearProps: 'all'
    });
    gsap.from('#vd-desc-right > *', {
        scrollTrigger: { trigger: '#vd-desc', start: 'top 76%', once: true },
        opacity: 0, y: 28, duration: .8, stagger: .12, delay: .2, ease: 'power3.out', clearProps: 'all'
    });

    /* Galerie — chaque item avec clip reveal */
    document.querySelectorAll('[data-gsap-gal]').forEach(function (el, i) {
        gsap.fromTo(el,
            { opacity: 0, y: 40 },
            {
                scrollTrigger: { trigger: '#vd-galerie', start: 'top 80%', once: true },
                opacity: 1, y: 0, duration: .7, delay: i * .08, ease: 'power3.out', clearProps: 'all'
            }
        );
    });

    /* Conciergerie items */
    gsap.from('.vd-conc-item', {
        scrollTrigger: { trigger: '.vd-conc', start: 'top 78%', once: true },
        opacity: 0, y: 24, duration: .6, stagger: .08, ease: 'power3.out', clearProps: 'all'
    });

    /* Similaires */
    gsap.from('.vd-sim-card', {
        scrollTrigger: { trigger: '.vd-similaires', start: 'top 80%', once: true },
        opacity: 0, y: 28, duration: .7, stagger: .12, ease: 'power3.out', clearProps: 'all'
    });

    /* CTA final */
    gsap.from('.vd-cta-inner > *', {
        scrollTrigger: { trigger: '.vd-cta-final', start: 'top 78%', once: true },
        opacity: 0, y: 24, duration: .7, stagger: .1, ease: 'power3.out', clearProps: 'all'
    });
});